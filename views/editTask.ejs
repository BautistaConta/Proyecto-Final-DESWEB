<!DOCTYPE html>
<html>
<head>
  <title>Editar Tarea</title>
</head>
<body>
  <h1>Editar tarea</h1>
  <form action="/tasks/editar/<%= tarea._id %>" method="POST">
    <input type="text" name="title" value="<%= tarea.title %>" required />
    <input type="text" name="description" value="<%= tarea.description %>" />
    <label>
      <input type="checkbox" name="completed" <%= tarea.completed ? "checked" : "" %> />
      Completada
    </label>
    <button type="submit">Guardar</button>
  </form>
  <a href="/tasks">Volver</a>
</body>
</html>
